# Diapo

```bash
cargo install --locked typst-cli
```

Puis
```bash
typst compile main.typ
```


Illustrations:
```bash
mlir-opt --view-op-graph boucle.mlir
opt -passes=dot-cfg boucle.ll -cfg-dot-filename-prefix=temp
```
